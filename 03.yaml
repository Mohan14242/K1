apiVersion: v1 
kind: Pod 
metadata: 
  name: multi-container-pod 
  namespace: dev-pod 
spec: 
  containers:
    - name: nginx 
      image: nginx 
      ports:
        - containerPort: 80 
    - name: sidecar 
      image: nginx
